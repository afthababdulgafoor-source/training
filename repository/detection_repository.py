insertvaluequery="INSERT INTO VEHICLE_DETECTION(vehicle_number,camera_id,detection_time,confidence_score) VALUES(%s,%s,%s,%s)"
vehiclelistingquery="SELECT * FROM VEHICLE_DETECTION"
listbyvehicleumber="SELECT * FROM VEHICLE_DETECTION WHERE VEHICLE_NUMBER=%s"
deletebyvehiclenumber="DELETE FROM VEHICLE_DETECTION WHERE VEHICLE_NUMBER=%s"
counttotaldetections="SELECT COUNT(*) AS TOTAL_DETECTIONS FROM VEHICLE_DETECTION"
confidencescorelisting="SELECT * FROM VEHICLE_DETECTION WHERE CONFIDENCE_SCORE>90"
deletebyvehiclecount="DELETE FROM VEHICLE_DETECTION WHERE VEHICLE_NUMBER IN (SELECT VEHICLE_NUMBER FROM (SELECT VEHICLE_NUMBER FROM VEHICLE_DETECTION GROUP BY VEHICLE_NUMBER HAVING COUNT(*) > 1) as T )"
counttotaldetections="SELECT COUNT(*) AS TOTAL_DETECTIONS FROM VEHICLE_DETECTION"
vehiclecheck="SELECT * FROM VEHICLE_DETECTION WHERE VEHICLE_NUMBER=%s"
